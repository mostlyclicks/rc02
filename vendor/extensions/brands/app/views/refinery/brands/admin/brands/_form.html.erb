<%= form_for [refinery, :brands_admin, @brand] do |f| -%>
  <%= render '/refinery/admin/error_messages',
              :object => @brand,
              :include_object_name => true %>

  <div class='field'>
    <%= f.label :name -%>
    <%= f.text_field :name, :class => 'larger widest' -%>
  </div>

  <div class='field'>
    <%= f.label :logo_image -%>
    <%= render '/refinery/admin/image_picker',
               :f => f,
               :field => :logo_image_id,
               :image => @brand.logo_image,
               :toggle_image_display => false -%>
  </div>

  <div class='field'>
    <%= f.label :main_image -%>
    <%= render '/refinery/admin/image_picker',
               :f => f,
               :field => :main_image_id,
               :image => @brand.main_image,
               :toggle_image_display => false -%>
  </div>

  <div class='field'>
    <%= render '/refinery/admin/wysiwyg',
                :f => f,
                :fields => [:intro, :description],
                :object => "brands/brand" -%>
  </div>

  <%= render '/refinery/admin/form_actions', :f => f,
             :continue_editing => false,
             :delete_title => t('delete', :scope => 'refinery.brands.admin.brands.brand'),
             :delete_confirmation => t('message', :scope => 'refinery.admin.delete', :title => @brand.name) -%>
<% end -%>

<% content_for :javascripts do -%>
  <script>
    $(document).ready(function(){
      page_options.init(false, '', '');
    });
  </script>
<% end -%>
